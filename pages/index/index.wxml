<view class="home">
  <!-- 顶部渐变头：问候 + 等级 + 成长值 -->
  <view class="hero">
    <view class="hero-blob hero-blob-1"></view>
    <view class="hero-blob hero-blob-2"></view>
    <view class="hero-row">
      <view class="hero-user" bindtap="onSwitchRole">
        <block wx:if="{{viewRole === 'parent'}}">
          <view class="hero-avatar">👨‍👩‍👧</view>
          <view class="hero-text">
            <view class="hero-hi">家长<text class="hero-switch">切换 ▾</text></view>
            <view class="hero-meta">管控视角</view>
          </view>
        </block>
        <block wx:else>
          <image wx:if="{{childAvatar}}" src="{{childAvatar}}" class="hero-avatar hero-avatar-img" mode="aspectFill" />
          <view wx:else class="hero-avatar">🧒</view>
          <view class="hero-text">
            <view class="hero-hi">Hi，{{childName}}！<text class="hero-switch">切换 ▾</text></view>
            <view class="hero-meta">Lv.{{level}}<text class="hero-dot">·</text>成长值 {{gameMinutes}}</view>
          </view>
        </block>
      </view>
      <view class="hero-badge" bindtap="onTapFamily">
        <text class="hero-badge-icon">👨‍👩‍👧</text>
      </view>
    </view>

    <!-- 今日能量(孩子视角) -->
    <view class="energy" wx:if="{{viewRole !== 'parent'}}">
      <view class="energy-top">
        <text class="energy-label">今日任务能量</text>
        <text class="energy-val">{{doneCount}}/3</text>
      </view>
      <view class="energy-bar"><view class="energy-fill" style="width:{{progressPct}}%"></view></view>
      <view class="energy-tip">今日在线学习 {{studyMinutes}} 分钟 · 完成打卡与小测点亮能量 ⚡</view>
    </view>
  </view>

  <!-- ============ 家长视角(REQ-015)：学习报告/时长统计 + 管控入口 ============ -->
  <block wx:if="{{viewRole === 'parent'}}">
    <view class="section-title">学习报告 · 时长统计</view>
    <view class="p-stats">
      <view class="p-stat"><view class="p-val">{{studyMinutes}}</view><view class="p-lab">今日在线(分)</view></view>
      <view class="p-stat"><view class="p-val">{{usageTodayDwellMin}}</view><view class="p-lab">今日练习(分)</view></view>
      <view class="p-stat"><view class="p-val">{{usageTodayTaps}}</view><view class="p-lab">练习互动(次)</view></view>
    </view>
    <view class="p-stats">
      <view class="p-stat"><view class="p-val">{{gameMinutes}}</view><view class="p-lab">游戏时间(分)</view></view>
      <view class="p-stat"><view class="p-val">{{wrongMastered}}/{{wrongTotal}}</view><view class="p-lab">错题已巩固</view></view>
      <view class="p-stat"><view class="p-val">{{doneCount}}/3</view><view class="p-lab">今日打卡</view></view>
    </view>

    <view class="section-title">管控</view>
    <view class="func-list">
      <view class="func-item" bindtap="onGoAccount">
        <view class="func-icon-wrap math-icon"><text class="func-icon">📊</text></view>
        <view class="func-info"><view class="func-name">家长报告 / 游戏时间</view><view class="func-desc">余额、今日/本周、练习互动</view></view>
        <view class="func-arrow">›</view>
      </view>
      <view class="func-item" bindtap="onTapWrong">
        <view class="func-icon-wrap exam-icon"><text class="func-icon">📒</text></view>
        <view class="func-info"><view class="func-name">错题本管理</view><view class="func-desc">查看/编辑错题(需家长密码)</view></view>
        <view class="func-arrow">›</view>
      </view>
      <view class="func-item" bindtap="onTapFamily">
        <view class="func-icon-wrap pinyin-icon"><text class="func-icon">👨‍👩‍👧</text></view>
        <view class="func-info"><view class="func-name">家庭管理</view><view class="func-desc">成员/小孩/家庭设置</view></view>
        <view class="func-arrow">›</view>
      </view>
      <view class="func-item" bindtap="onGoSettings">
        <view class="func-icon-wrap english-icon"><text class="func-icon">⚙️</text></view>
        <view class="func-info"><view class="func-name">设置</view><view class="func-desc">账户/分享/清除数据</view></view>
        <view class="func-arrow">›</view>
      </view>
    </view>
  </block>

  <!-- ============ 孩子视角：学习内容 ============ -->
  <block wx:if="{{viewRole !== 'parent'}}">
  <!-- 三大入口 -->
  <view class="entry-grid">
    <view class="entry-card entry-green" bindtap="onTapReward">
      <view class="entry-emoji">📅</view>
      <view class="entry-title">学习习惯</view>
      <view class="entry-sub">每日打卡养成</view>
    </view>
    <view class="entry-card entry-orange" bindtap="onTapRecipe">
      <view class="entry-emoji">🥗</view>
      <view class="entry-title">营养饮食</view>
      <view class="entry-sub">菜谱与搭配</view>
    </view>
    <view class="entry-card entry-purple" bindtap="onTapWrong">
      <view class="entry-emoji">📒</view>
      <view class="entry-title">错题本</view>
      <view class="entry-sub">错题统一复习</view>
    </view>
  </view>

  <!-- 成长轨迹(REQ-017 迁自设置页 + REQ-018 综合内容) -->
  <view class="growth">
    <view class="growth-head">
      <text class="growth-title">🌱 成长轨迹</text>
      <text class="growth-sub">最近的学习与生活表现</text>
    </view>
    <view class="growth-stats">
      <view class="g-stat">
        <view class="g-val">{{likedCount}}</view>
        <view class="g-lab">爱吃的菜</view>
      </view>
      <view class="g-stat">
        <view class="g-val">{{ratingCount}}</view>
        <view class="g-lab">我的评分</view>
      </view>
      <view class="g-stat">
        <view class="g-val">{{wrongMastered}}/{{wrongTotal}}</view>
        <view class="g-lab">错题已巩固</view>
      </view>
    </view>
    <view class="growth-dishes" wx:if="{{topDishes.length > 0}}">
      <view class="g-dish" wx:for="{{topDishes}}" wx:key="id">
        <text class="g-dish-name">🍽️ {{item.name}}</text>
        <text class="g-dish-score">{{item.avgScore}}分</text>
      </view>
    </view>
    <view class="growth-empty" wx:if="{{!hasGrowth}}">多记录菜谱评分与错题，这里会长出你的成长轨迹～</view>
  </view>

  <!-- 一键小测 CTA -->
  <view class="quiz-cta" bindtap="onTapDailyQuiz">
    <view class="quiz-left">
      <view class="quiz-emoji">🚀</view>
      <view class="quiz-textwrap">
        <view class="quiz-title">今日一键小测</view>
        <view class="quiz-sub">随机一科 · 约 3 分钟，完成赚能量</view>
      </view>
    </view>
    <view class="quiz-go">开始 ›</view>
  </view>

  <!-- 学习练习 -->
  <view class="section-title">学习练习</view>
  <view class="func-list">
    <view class="func-item" bindtap="onTapMath">
      <view class="func-icon-wrap math-icon"><text class="func-icon">🧮</text></view>
      <view class="func-info">
        <view class="func-name">数学速算</view>
        <view class="func-desc">10/20 以内加减法</view>
      </view>
      <view class="func-arrow">›</view>
    </view>
    <view class="func-item" bindtap="onTapPinyin">
      <view class="func-icon-wrap pinyin-icon"><text class="func-icon">📖</text></view>
      <view class="func-info">
        <view class="func-name">语文拼音</view>
        <view class="func-desc">识字 / 写字练习</view>
      </view>
      <view class="func-arrow">›</view>
    </view>
    <view class="func-item" bindtap="onTapEnglish">
      <view class="func-icon-wrap english-icon"><text class="func-icon">🔤</text></view>
      <view class="func-info">
        <view class="func-name">英语测试</view>
        <view class="func-desc">词汇 / 句型 / 易混词</view>
      </view>
      <view class="func-arrow">›</view>
    </view>
    <view class="func-item" bindtap="onTapExam">
      <view class="func-icon-wrap exam-icon"><text class="func-icon">📷</text></view>
      <view class="func-info">
        <view class="func-name">拍照录题</view>
        <view class="func-desc">拍试卷照片 · 智能识别错题</view>
      </view>
      <view class="func-arrow">›</view>
    </view>
  </view>

  <!-- 习惯打卡：彩色软块网格 -->
  <view class="section-title">习惯打卡</view>
  <view class="habit-grid">
    <view class="habit-tile {{todayChecked.school ? 'done' : ''}}" bindtap="onTapReward">
      <view class="habit-tile-icon">🏫</view>
      <view class="habit-tile-name">到校</view>
      <view class="habit-tile-check">{{todayChecked.school ? '✓' : ''}}</view>
    </view>
    <view class="habit-tile {{todayChecked.homework ? 'done' : ''}}" bindtap="onTapReward">
      <view class="habit-tile-icon">📚</view>
      <view class="habit-tile-name">作业</view>
      <view class="habit-tile-check">{{todayChecked.homework ? '✓' : ''}}</view>
    </view>
    <view class="habit-tile {{todayChecked.sleep ? 'done' : ''}}" bindtap="onTapReward">
      <view class="habit-tile-icon">🌙</view>
      <view class="habit-tile-name">睡觉</view>
      <view class="habit-tile-check">{{todayChecked.sleep ? '✓' : ''}}</view>
    </view>
  </view>
  </block>
</view>
